# Base URL of the FastAPI backend. The frontend always talks to this backend —
# there is no mock fallback, so the backend must be running for the UI to load data.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
