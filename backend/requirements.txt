# Runtime dependencies (single source of truth).
# Install:  pip install -r requirements.txt
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
SQLAlchemy>=2.0.30
oracledb>=2.2
python-multipart>=0.0.9
httpx>=0.27
anthropic>=0.39
openai>=1.40
google-generativeai>=0.7
openpyxl>=3.1

# Dev/test toolchain. Includes runtime deps.
# Install:  pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.2
pytest-asyncio>=0.23
ruff>=0.4
mypy>=1.10

# Optional: real RAGAS scoring. When these are absent, ragas_service falls
# back to the deterministic local scorer (app/services/ragas/fallback_scorer.py).
# Install:  pip install -r requirements-ragas.txt
-r requirements.txt
ragas>=0.2,<0.3
datasets>=2.0
# ragas 0.2.x targets the langchain 0.3.x line. Leaving these unpinned let pip
# pull langchain 1.x (which relocated/removed modules ragas still imports, e.g.
# langchain_community.chat_models.vertexai) — `import ragas` then fails and the
# scorer silently falls back to FALLBACK. The judge uses langchain-openai.
langchain-core>=0.3,<0.4
langchain>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-openai>=0.2,<0.4